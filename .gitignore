# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Planning/progress (managed locally, not in repo)
.plan/

# Data (large/binary — published via Mendeley Data, not git history)
data/label_studio/*
data/raw/*
data/thumbnail/*
data/processed/*
data/release/
!data/**/.gitkeep
!data/README.md

# Private anonymization mapping (annotator ids — never publish)
*.private.json

# Working artifacts (local review lists, not repo content)
data/face_review_worklist.txt

# API keys / secrets
.env

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
