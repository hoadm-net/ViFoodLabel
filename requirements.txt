# OCR pre-annotation (scripts/preprocessing/)
torch>=2.1                          # backend for doctr / vietocr
Pillow>=10.0
python-doctr>=0.8                   # text detection
vietocr>=0.3.13                     # Vietnamese text recognition
opencv-python>=4.8                  # image prep (HEIC -> JPEG, thumbnails)

# Dataset construction, statistics, and release (scripts/, scripts/dataset/)
matplotlib>=3.8                     # statistics figures (compute_statistics.py)
gdown>=4.7                          # dataset image download
huggingface-hub>=0.23               # dataset publishing utility

# LLM-assisted QC linter (scripts/task3_qc_lint.py)
openrouter>=0.10                    # text linter via OpenRouter
python-dotenv>=1.0                  # load API key from .env
